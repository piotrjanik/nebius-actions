/** Public surface of the `io` module. */
export { getString, getBool, getNumber, getMultiline, getKeyValues } from './inputs';
export { setOutput, fail, normalizeError } from './outputs';
export { log, mask } from './log';
